[project]
name = "card-game-backend"
version = "0.1.0"
description = "Server for playing different multiplayer card games. Currently only 'Five Hundred' is supported."
authors = [{name = "Gatis M"}]
readme = "README.md"
requires-python = ">=3.12"

[tool.black]
line-length = 120